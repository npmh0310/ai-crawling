PORT=3010
HEADLESS=true
# Generate locally with: base64 -i cookies.json | pbcopy
# Paste the result here (Railway) or leave empty if mounting cookies.json file (docker-compose)
TWITTER_COOKIES_B64=
TWITTER_COOKIES_FILE=/app/cookies.json
